SELECT
	-- serverカラムを指定
	server,
	-- sample_dateカラムを指定
	sample_date,
	-- serverでパーティションを区切ってload_valの合計値を算出し、sum_loadの名前を付ける
	SUM(load_val) OVER (PARTITION BY server) AS sum_load
FROM
	-- ServerLoadSampleテーブルを指定
	ServerLoadSample
;

serverでパーティションを区切って合計値を求めているため、serverごとの負荷量の合計値が出力される