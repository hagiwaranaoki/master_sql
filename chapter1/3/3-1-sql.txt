SELECT
	-- P1の製品名をname_1として選択
	P1.name AS name_1, 
	-- P2の製品名をname_2として選択
	P2.name AS name_2
FROM
	-- productsテーブルをp1として参照
	products p1 
INNER JOIN 
		-- 同じproductsテーブルをp2として内部結合
		products p2
	ON
		-- p1の製品名がp2の製品名と同じか、アルファベット順で後ろにくる場合に結合条件とする
		p1.name >= p2.name
;